# EasyTier 玩法指南

> 一个由 Rust 和 Tokio 驱动的简单、安全、去中心化的异地组网方案

---

## 📋 目录

- [工具简介](#工具简介)
- [核心组件](#核心组件)
- [玩法一：公共节点快速组网](#玩法一公共节点快速组网)
- [玩法二：自建去中心化网络](#玩法二自建去中心化网络)
- [玩法三：子网代理共享局域网](#玩法三子网代理共享局域网)
- [玩法四：WireGuard 集成](#玩法四wireguard-集成)
- [玩法五：自建公共节点](#玩法五自建公共节点)
- [玩法六：Web 管理界面](#玩法六web-管理界面)
- [玩法七：游戏联机](#玩法七游戏联机)
- [玩法八：远程办公](#玩法八远程办公)
- [玩法九：NAS 远程访问](#玩法九nas-远程访问)
- [常用命令速查](#常用命令速查)
- [注意事项](#注意事项)

---

## 工具简介

EasyTier 是一个开源的 P2P VPN 工具，可以让你轻松地将多台设备组成一个虚拟局域网，无论它们在世界的哪个角落。

### 核心特性

| 特性 | 说明 |
|------|------|
| 🔒 去中心化 | 节点平等独立，无需中心服务器 |
| 🚀 易于使用 | 支持 Web、客户端、命令行多种方式 |
| 🌍 跨平台 | Win/Mac/Linux/Android，X86/ARM/MIPS |
| 🔐 安全加密 | AES-GCM 或 WireGuard 加密 |
| ⚡ 高性能 | 零拷贝，支持 TCP/UDP/WSS/WG 协议 |
| 🔌 NAT 穿透 | 支持 UDP 和 IPv6 穿透 |

---

## 核心组件

| 组件 | 说明 |
|------|------|
| `easytier-core` | 核心程序，负责建立虚拟网络 |
| `easytier-cli` | 命令行工具，查看网络状态 |
| `easytier-web` | Web 管理界面 |
| `easytier-gui` | 图形界面客户端 |

---

## 玩法一：公共节点快速组网

> 🎯 适用场景：没有公网 IP，想快速组网

这是最简单的玩法，利用官方提供的免费公共节点作为中继。

### 步骤

**设备 A（以管理员权限运行）：**
```powershell
easytier-core -d --network-name 我的网络 --network-secret 密码123 -p tcp://public.easytier.cn:11010
```

**设备 B（以管理员权限运行）：**
```powershell
easytier-core -d --network-name 我的网络 --network-secret 密码123 -p tcp://public.easytier.cn:11010
```

### 参数说明

| 参数 | 说明 |
|------|------|
| `-d` | 后台运行（daemon 模式） |
| `--network-name` | 网络名称，同一网络的设备需一致 |
| `--network-secret` | 网络密码，同一网络的设备需一致 |
| `-p` | 公共节点地址 |

### 验证连接

```powershell
# 查看节点状态
easytier-cli peer

# 测试连通性
ping 10.126.126.1
```

### 提高可用性

连接多个公共节点：
```powershell
easytier-core -d --network-name 我的网络 --network-secret 密码123 -p tcp://public.easytier.cn:11010 -p udp://public.easytier.cn:11010
```

---

## 玩法二：自建去中心化网络

> 🎯 适用场景：有公网 IP，想要更低延迟和更高隐私

### 网络拓扑

```
设备 A (公网 IP: 1.2.3.4)
    ↓
设备 B ←→ 设备 C ←→ 设备 D
```

### 步骤

**设备 A（有公网 IP）：**
```powershell
easytier-core -i 10.144.144.1
```

启动后监听端口：
- TCP: 11010
- UDP: 11010
- WebSocket: 11011
- WebSocket SSL: 11012
- WireGuard: 11013

**设备 B：**
```powershell
easytier-core -i 10.144.144.2 -p udp://1.2.3.4:11010
```

**设备 C：**
```powershell
# 可以连接任意已在网络中的节点
easytier-core -i 10.144.144.3 -p udp://1.2.3.4:11010
```

### 验证

```powershell
easytier-cli peer    # 查看节点
easytier-cli route   # 查看路由
easytier-cli node    # 查看本地信息
```

---

## 玩法三：子网代理共享局域网

> 🎯 适用场景：让远程设备访问本地局域网内的其他设备

### 网络拓扑

```
远程设备 A ←→ 家里设备 B ←→ 家里局域网 (192.168.1.0/24)
                              ├── NAS (192.168.1.100)
                              ├── 打印机 (192.168.1.101)
                              └── 其他设备...
```

### 步骤

**家里设备 B（代理本地网络）：**
```powershell
easytier-core -i 10.144.144.2 -n 192.168.1.0/24 -p tcp://public.easytier.cn:11010 --network-name home --network-secret 123456
```

**远程设备 A：**
```powershell
easytier-core -d --network-name home --network-secret 123456 -p tcp://public.easytier.cn:11010
```

### 验证

```powershell
# 查看路由（proxy_cidrs 列显示代理的子网）
easytier-cli route

# 访问家里的 NAS
ping 192.168.1.100
```

---

## 玩法四：WireGuard 集成

> 🎯 适用场景：让 iOS/Android 设备通过 WireGuard 客户端接入网络

### 步骤

**1. 启动 EasyTier 并开启 WireGuard 门户：**
```powershell
easytier-core -i 10.144.144.1 --vpn-portal wg://0.0.0.0:11013/10.14.14.0/24
```

**2. 获取 WireGuard 配置：**
```powershell
easytier-cli vpn-portal
```

**3. 配置 WireGuard 客户端：**
- 将 `Interface.Address` 设置为 `10.14.14.x`（x 为 2-254）
- 将 `Peer.Endpoint` 设置为服务器公网 IP:11013
- 导入配置到 WireGuard 客户端

### 支持的客户端

- iOS WireGuard
- Android WireGuard
- Windows WireGuard
- macOS WireGuard

---

## 玩法五：自建公共节点

> 🎯 适用场景：有闲置服务器，想帮助他人或自用

### 步骤

在有公网 IP 的服务器上运行：
```bash
sudo easytier-core --network-name my-public-node --network-secret my-public-node
```

其他用户可以通过以下方式连接：
```powershell
easytier-core -d --network-name 网络名 --network-secret 密码 -p tcp://你的服务器IP:11010
```

---

## 玩法六：Web 管理界面

> 🎯 适用场景：通过浏览器管理和监控网络

### 启动 Web 服务

```powershell
easytier-web
```

### 功能

- 可视化网络拓扑
- 实时监控节点状态
- 配置管理
- 流量统计

---

## 玩法七：游戏联机

> 🎯 适用场景：和朋友联机玩局域网游戏

### 步骤

**1. 所有玩家使用相同的网络名和密码：**
```powershell
easytier-core -d --network-name minecraft-server --network-secret game123 -p tcp://public.easytier.cn:11010
```

**2. 查看分配的虚拟 IP：**
```powershell
easytier-cli node
```

**3. 在游戏中使用虚拟 IP 连接**

### 适用游戏

- Minecraft（局域网联机）
- 泰拉瑞亚
- 饥荒
- 其他支持局域网的游戏

---

## 玩法八：远程办公

> 🎯 适用场景：在家访问公司内网资源

### 网络拓扑

```
家里电脑 ←→ 公司电脑 ←→ 公司内网
                         ├── 文件服务器
                         ├── 内部系统
                         └── 数据库
```

### 步骤

**公司电脑（代理公司网络）：**
```powershell
easytier-core -i 10.144.144.1 -n 10.0.0.0/8 -p tcp://public.easytier.cn:11010 --network-name work --network-secret office123
```

**家里电脑：**
```powershell
easytier-core -d --network-name work --network-secret office123 -p tcp://public.easytier.cn:11010
```

---

## 玩法九：NAS 远程访问

> 🎯 适用场景：在外面访问家里的 NAS

### 步骤

**1. 在 NAS 上安装 EasyTier（如果支持）或在同网络的设备上运行：**
```bash
easytier-core -i 10.144.144.1 -n 192.168.1.0/24 -p tcp://public.easytier.cn:11010 --network-name nas --network-secret nas123
```

**2. 在外出设备上：**
```powershell
easytier-core -d --network-name nas --network-secret nas123 -p tcp://public.easytier.cn:11010
```

**3. 通过 NAS 的局域网 IP 访问：**
```
http://192.168.1.100:5000  # 群晖
http://192.168.1.100       # 其他 NAS
```

---

## 常用命令速查

| 命令 | 说明 |
|------|------|
| `easytier-cli peer` | 查看连接的节点 |
| `easytier-cli route` | 查看路由信息 |
| `easytier-cli node` | 查看本地节点信息 |
| `easytier-cli vpn-portal` | 获取 WireGuard 配置 |
| `easytier-cli connector` | 查看连接器状态 |

### 常用参数

| 参数 | 说明 |
|------|------|
| `-i <IP>` | 指定虚拟 IP 地址 |
| `-d` | 后台运行模式 |
| `-p <URL>` | 指定对等节点地址 |
| `-n <CIDR>` | 代理子网 |
| `--network-name` | 网络名称 |
| `--network-secret` | 网络密码 |
| `--vpn-portal` | 启用 WireGuard 门户 |
| `-l <PORT>` | 指定监听端口 |

---

## 注意事项

### ⚠️ 必须以管理员权限运行

EasyTier 需要创建虚拟网卡，必须以管理员权限运行：
- **Windows**: 右键 → 以管理员身份运行
- **Linux/Mac**: 使用 `sudo`

### 🔥 防火墙设置

如果无法连接，检查防火墙：
- 允许 `easytier-core` 通过防火墙
- 开放端口：11010 (TCP/UDP)、11011-11013

### 🔐 安全建议

- 使用复杂的网络名和密码
- 避免使用简单的名称如 `abc`、`test`
- 定期更换密码

### 📊 性能优化

- 优先使用 UDP 协议（延迟更低）
- 在高丢包环境使用 KCP/QUIC 代理
- 连接多个公共节点提高可用性

---

## 相关资源

- 📚 [官方文档](https://easytier.cn)
- 🖥️ [Web 控制台](https://easytier.cn/web)
- 📝 [下载地址](https://github.com/EasyTier/EasyTier/releases)
- 💬 [Telegram 群组](https://t.me/easytier)
- 👥 QQ 群：949700262 / 837676408 / 957189589

---

*本文档基于 EasyTier v2.4.5 编写*
